# Node.js App

A simple Express.js application.

## Running Locally

1. Install dependencies: `npm install`
2. Run: `npm start`
3. Dev mode with auto-reload: `npm run dev`

## Docker

Build: `docker build -t nodejs-app .`

Run: `docker run -p 3000:3000 nodejs-app`

Visit: http://localhost:3000
